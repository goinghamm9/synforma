# Netlify: Synforma on the Next.js runtime (the optional planner route needs a server).
# Used when the project's base directory is this folder. Paths are relative to this folder.
[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
